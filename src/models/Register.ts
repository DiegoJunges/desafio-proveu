class Register {
  initialHour: Date;

  finalHour: Date;

  constructor(initialHour: Date, finalHour: Date) {
    this.initialHour = initialHour;
    this.finalHour = finalHour;
  }
}

export default Register;
